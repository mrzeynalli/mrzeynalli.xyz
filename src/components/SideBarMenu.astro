---
const { sideBarActiveItemID } = Astro.props;
const activeClass = "bg-base-300 font-semibold";

const isActive = (id: string) => sideBarActiveItemID === id;
---

<ul class="menu menu-md px-0 py-4 gap-0.5">
  <li>
    <a
      id="home"
      href="/"
      class:list={["py-3 px-6 text-base rounded-none", { [activeClass]: isActive("home") }]}
    >
      Home
    </a>
  </li>

  <li>
    <a
      id="projects"
      href="/projects"
      class:list={["py-3 px-6 text-base rounded-none", { [activeClass]: isActive("projects") }]}
    >
      Projects
    </a>
  </li>

  <!-- Sub-items under Projects -->
  <li>
    <a
      id="analytics"
      href="/analytics"
      class:list={[
        "py-2 px-6 ps-10 text-sm rounded-none text-base-content/70 hover:text-base-content",
        { [activeClass]: isActive("analytics") },
      ]}
    >
      → Analytics
    </a>
  </li>
  <li>
    <a
      id="quant-research"
      href="/quant-research"
      class:list={[
        "py-2 px-6 ps-10 text-sm rounded-none text-base-content/70 hover:text-base-content",
        { [activeClass]: isActive("quant-research") },
      ]}
    >
      → Quant Research
    </a>
  </li>
  <li>
    <a
      id="automation"
      href="/automation"
      class:list={[
        "py-2 px-6 ps-10 text-sm rounded-none text-base-content/70 hover:text-base-content",
        { [activeClass]: isActive("automation") },
      ]}
    >
      → Automation
    </a>
  </li>

  <li>
    <a
      id="archives"
      href="/archives"
      class:list={["py-3 px-6 text-base rounded-none", { [activeClass]: isActive("archives") }]}
    >
      Archive
    </a>
  </li>

  <li>
    <a
      id="search"
      href="/search"
      class:list={["py-3 px-6 text-base rounded-none", { [activeClass]: isActive("search") }]}
    >
      Search
    </a>
  </li>

  <li>
    <a
      id="about"
      href="/about"
      class:list={["py-3 px-6 text-base rounded-none", { [activeClass]: isActive("about") }]}
    >
      Resume
    </a>
  </li>

  <li>
    <a
      id="contact"
      href="mailto:ezeynalli@hotmail.com"
      class="py-3 px-6 text-base rounded-none"
    >
      Contact
    </a>
  </li>
</ul>
